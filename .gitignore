# Dependencies
node_modules

# Build output
dist
dist-ssr
coverage

# Local env files (never commit secrets) — the example is tracked
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
*.local
.vscode/*
!.vscode/extensions.json
.idea

# Supabase local state (never commit) — schema/migrations ARE committed in Phase 2
supabase/.branches
supabase/.temp
supabase/.env
